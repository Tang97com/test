
<view class='itembox'>
  <!-- <view class='scorePin'>{{starId}}星</view> -->
  <view wx:for="{{imgs}}" wx:key="index" bindtap='select' data-index="{{item.id}}">
    <image class="star" src="{{item.id>starId?src2:src1}}"></image>
  </view>
</view>